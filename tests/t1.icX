var a = 2
var b = 0
var hash
var ms
a++
alias d0 furnace

const testDef = 5

if a == 0 then
    fn1()
else
    fn2()
end

if a == 0 then
    fn1()
end

b=a

b = 0

while a > 0
	d0.Setting = i
	a++
end

function fn1
	d0.Setting = 1 # запись в d0 setting
	i = d0.Setting
	furnace.Setting = 1 # запись в d0 setting
	hash = d0.slot(0).hash # чтение из слота 0 из устройства d0  параметра hash
	ms = d(hash).Setting(Maximum) #пакетное чтение из портов d0-d5 максимального значение Setting
end

def fn2


end
