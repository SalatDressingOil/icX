use comments
use aliases
use constants
var x = 3
const y = 4

function farrago(a, b =10)
	a = (b*b) + (a*a)
	b = sqrt(a)
	return b
end

var result = farrago(x,y)
debug result

var result2 = farrago(9,6)
debug result2